package com.finshield.backend.config;

/**
 * Kept as a migration marker. Development now uses the same security boundary as every other profile.
 */
@Deprecated(forRemoval = true)
final class DevSecurityConfig {

    private DevSecurityConfig() {
    }
}
